# 이 저장소에서 일할 때

산업안전 AI 서비스(센서 이상 감지 + 점검 체크리스트)의 **UI/UX 연구** 저장소다. 공장에 가 보거나 사용자를 만날 수 없어서 문헌·원칙으로 대신하고, 짐작한 것은 `가설`로 표시한다.

## 먼저 읽을 것

1. `README.md` — 진행 상황과 길잡이
2. `260914_UIUX_개발_방법론.md` — **정본.** 맨 앞 "지금 어디까지 왔나"와 "### 남은 일" 절
3. `시제품/README.md` — 시제품 띄우는 법과 최근 바뀐 것

화면의 최신본은 `시제품/`이다. `4단계_시안/`의 HTML은 결정 기록으로 남겨 둔 것이라 고치지 않는다.

## 사용자와 일하는 법

- **기존 화성 산업안전지킴이 웹의 방식을 따른다** (2026-09-22 사용자 결정). 이 서비스는 그 웹을 쓰는 화성산업진흥원에 들어간다. 우리가 먼저 정한 흐름과 부딪히면 기존 방식이 이긴다. 기존 방식은 `260921_기존시스템_분석.md`.
- **흐름·기능 변경은 사용자가 정한다.** 더 나은 안이 보이면 제안하되, 묻지 않고 반영하지 않는다.
- **설명은 쉬운 말로 짧게.** 결론 한 줄 → 비유나 텍스트 그림 → 필요하면 작은 표. 용어는 그 자리에서 한 줄로 푼다. 코드를 길게 인용하지 말고 설계 관점(왜 필요했나 · 다른 길은 · 안 그러면 무엇이 깨지나)으로, 코드는 `파일:줄` 포인터 정도만.
- **모르는 것을 만나면** "이걸 모르면 화면이 ①틀리게 그려지나 ②아예 못 그리나 ③그대로인가"만 묻는다. ①②는 지금 정하고 ③은 `가설`로 적고 진행한다. 이 기준 없이 가면 화면 연구가 서비스 전체 설계로 번진다.
- **커밋 메시지는 한 줄.** `fix:`·`feat:`·`docs:`·`chore:` + 한국어 제목. 본문은 꼭 필요할 때만 두세 줄.
- **날짜를 적기 전에 시스템 날짜를 확인한다.** 이 저장소는 날짜로 결정 순서를 따라간다.
- **저장소가 Public이다.** 비밀번호·개인정보·내부 사정을 넣지 않는다.

## 화면 규칙 (이미 정한 것)

- 화면 번호는 앱 머리글자 + 차례 — **W** 근로자 앱 · **G** 지킴이 앱 · **O** 공장주 웹 · **M** 운영자 웹. 한 화면의 칸은 `-가`·`-나`.
- **UI에 지역 이름을 쓰지 않는다.**
- 실증 규모는 공장 3곳 × 센서 5개 안팎. 예시도 이 규모로.
- **근로자는 센서 값을 보지 않는다**(받은 경보 기록에도 값 없음). 경보 화면에는 닫기가 없고, 경보 알림을 끄는 스위치도 두지 않는다.
- 못 누르는 것을 회색 잠긴 칸으로 두지 않는다 — 칸을 없앤다. 고르는 모양(체크 칸)은 AI 제안에만 쓴다.
- 지킴이 점검은 **기본 체크리스트가 먼저**, AI 제안은 원할 때 더한다. 답은 `이상 없음 / 문제 있음 / 해당 없음` 셋.
- 시제품은 UI/UX 연구 도구다. 서버·DB·인증은 "적당히 돌아갈 정도"로만. 아이디·비밀번호 로그인은 **시험 편의**일 뿐, 설계는 그대로 "공장주는 휴대폰 번호 인증"이다.

## 시제품 다루기

- 띄우기: `시제품/`에서 `python server.py --port 8765` (리눅스·맥은 `./serve.sh`, 윈도우 PowerShell에서는 `serve.sh`가 안 돈다).
- **사용자가 켜 둔 서버의 데이터를 건드리지 않는다.** 시험은 시제품을 복사해 다른 포트로(`--reset`) 띄워서 한다.
- 시험: `_시험/flow.js` · `worker_flow.js` · `cross_flow.js` (Playwright, 실행법은 `시제품/README.md`). `flow.js`의 401·409 콘솔 오류는 원래 나오는 것이다(로그인 전 호출 · 여러 기기 판 충돌).
- CSS에 새 이름을 붙이기 전에 기존 이름과 겹치는지 찾는다. `.cnt`·`.done`이 겹쳐 화면이 깨진 적이 있다.
- 전체 페이지 캡처에서 위에 붙는 머리글·아래 버튼이 내용 위에 겹쳐 보이는 것은 캡처 탓이다. 실제 배치는 요소 위치를 재서 확인한다.
- 방법론 문서의 흐름도(mermaid)를 고치면 실제로 그려서 확인한다. 곁길은 노드가 아니라 서브그래프끼리 이어야 읽힌다.
- 마크다운에서 굵게를 닫는 `**` 바로 앞이 구두점이고 뒤에 한글이 붙으면(`**…)**이라`) 별표가 그대로 보인다. 구두점은 굵게 바깥으로 뺀다.
