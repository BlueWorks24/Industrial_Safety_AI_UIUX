# 시제품 원칙 점검 근거 (2026-09-17)

`시제품/260917_시제품_원칙점검.md`의 근거 자료다. 고치기 전 시제품의 모습이다.

| 폴더 | 내용 |
|---|---|
| `캡처_공통/` | 시험 세 벌(`flow.js` 01~51 · `cross_flow.js` c01~c28 · `worker_flow.js` w01~w28)로 찍은 화면 107장. 390×780 |
| `경보_ISA/` | 캡처에 없던 경보 상황(s1 경보 넷 · s2 완료 화면 중 새 경보 · s3 끊김+경보 · s4 2시간 방치 · s5 보는 중 수리 중 · s6 수리 중·모름 정상 · s7 숫자 없는 작업 예정) 캡처와 스크립트 `isa.js`·`isa2.js` |
| `닐슨/캡처/` | 다시 확인한 캡처 r1~r5(완료 화면 중 다시 알림·새 경보, 끊김 홈 등) |
| `접근성/` | 측정 스크립트(`run.js` 화면별 대비·크기·키보드·aria 측정, `audit_lib.js`, `textzoom.js` 글자 150·200%, `opnote.js` 다시 그리기 때 입력 지워짐), `측정값/res_*.json`(390·320·1280 폭), `캡처/` |

다시 돌리기: 시제품 복사본을 따로 띄운다(켜 둔 8765 데이터가 바뀌지 않게). 스크립트 안의 주소(8772·8774)와 저장 위치(`접근성`은 스크립트 옆 `out/`)를 맞춘 뒤 `NODE_PATH=$(npm root -g) node <스크립트> <캡처 폴더>`.
